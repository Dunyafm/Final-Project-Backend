@model List<Slider>

@foreach (var slider in Model)
{
    <div class="carousel-item">
        <div class="carousel-image">
            <img src="~/assets/img/slider/@slider.Image" style="width:100%;height:130%" alt="Alternate Text" />
            
        </div>
    </div>
}